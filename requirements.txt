# Core data pipeline
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0

# Portfolio optimizer
scipy>=1.11.0
hmmlearn>=0.3.0   # optional — graceful fallback if absent

# Dashboard (app.py)
streamlit>=1.30.0
plotly>=5.18.0

# PDF report export (report.py)
reportlab>=4.0.0

# Machine-learning models (Random Forest signal in app.py Quant Models tab)
scikit-learn>=1.3.0

# Strategy library (strategies.py) — all stdlib, no extra deps

# Production execution engine (trading_bot/) — async brokers, tests
aiohttp>=3.9.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
# Requires Python 3.11+
